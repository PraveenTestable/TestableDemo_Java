[pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    benchmark: performance benchmark tests (deselect with '-m "not benchmark"')
    integration: integration tests requiring file system access
    mutation: mutation testing related guards
    slow: tests that take longer than 1 second
